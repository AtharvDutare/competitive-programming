int ETF(int n) {
    int phi_n = n;
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            while (n % i == 0)
                n /= i;
            phi_n -= phi_n / i;
        }
    }
    if (n > 1)
        phi_n -= phi_n / n;
    return result;
}
vector<int> ETF_1_to_n(int n) {
    vector<int> phi(n + 1);
    for (int i = 0; i <= n; i++)
        phi[i] = i;

    for (int i = 2; i <= n; i++) {
        if (phi[i] == i) {
            for (int j = i; j <= n; j += i)
                phi[j] -= phi[j] / i;
        }
    }
  return phi
}